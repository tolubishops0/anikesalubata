import React from "react";

function ProductListingDetailPage({ otherProductList }) {
  console.log(otherProductList);
  return <div> this is the details listing page</div>;
}

export default ProductListingDetailPage;
