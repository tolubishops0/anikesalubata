import React from "react";

function AuthState() {
  const initialState = {
    isLoggedIn: false,
    email: null,
    userName: null,
    userId: null,
  };

  return <div></div>;
}

export default AuthState;
